# Basic Online Store project built by using PERN stack (PostgreSQL, Node.js, Express, React+mobx)

To run client, enter:

### `cd client`

Then:

### `npm start`

To run server, enter: 

### `cd server`

Then:

### `npm run dev`
